PROGRAM step4
BEGIN

	INT a, b, i, p;

--	PROTO VOID main();

	FUNCTION VOID main()
	BEGIN

--		i := 0;
		a := 0;
		b := 0;

--		REPEAT
--			READ (p);
--			
--			IF (p > 10) 
--				THEN a := a + 1;
--				ELSE b := b + 1;
--			ENDIF;
--
--			i := i+1;
--
--		UNTIL (i > 10);

		FOR (i:=0; i<11; i:=i+1)
			READ (p);
			
			IF (p > 10)	THEN 
				a := a + 1;
			ELSE
				b := b + 1;
			ENDIF
		
		ENDFOR

		WRITE (a);
		WRITE (b);
	END
END

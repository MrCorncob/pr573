PROGRAM test
BEGIN
	INT a,b,c,x,y,z;
FLOAT h,j,k;
      INT gg, tt;
	STRING dummy := "dummy";
	--PROTO VOID global (INT a,INT b,INT c);
	--PROTO VOID Global (INT a,INT b,INT c);
	--PROTO INT main();
	FUNCTION VOID global (INT a,INT b,INT c)
	BEGIN
		WRITE (a);
		WRITE (b);
		WRITE (c);
	END
	FUNCTION VOID Global (INT a,INT b,INT c)
	BEGIN
		WRITE (a);
		WRITE (b);
		WRITE (c);
	END
  FUNCTION INT main()
	BEGIN
  	STRING a :="";
	INT xyz; 
	IF (a = b)
	THEN
		
		h:=0;
	ELSE
	
--		j:=1;
--		REPEAT
--			a := j;
--			j := j + 1;		
--		UNTIL(j>10);

		FOR (j:=1; j<11; j:=j+1)
			a := j;
		ENDFOR

	ENDIF
	RETURN a+b;
	END
END
